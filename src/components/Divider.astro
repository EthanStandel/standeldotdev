---

---
<hr />
<style>
  hr {
    all: unset;
    position: relative;
    top: calc(100% + 10px);
    width: 100vw;
    left: calc(-50vw + 50%);
    right: 0;
    height: 5px;
    margin-top: 1em;
    margin-bottom: 1em;
    background: linear-gradient(
      to right,
      var(--palette-primary),
      var(--palette-secondary)
    );
  }
</style>